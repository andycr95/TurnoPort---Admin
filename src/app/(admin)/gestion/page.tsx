"use client";
// import node module libraries
import Link from "next/link";
import { Col, Row, Container, Image } from "react-bootstrap";

const Layout = () => {
  return <Container fluid className="px-6 py-4"></Container>;
};

export default Layout;
